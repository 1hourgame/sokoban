import Phaser from "phaser";


export class MainScene extends Phaser.Scene {
  constructor() {
    super({ key: "Game", active: true });
  }

  init(data) {
  }

  preload() {
  }

  create() {
  }

  update() {}
}
